# 🚀 Local Chat

**Local Chat** — это современное веб-приложение для общения с локальными LLM (через n8n/Ollama). Проект построен на микросервисной архитектуре с использованием Docker, React и Node.js.

![Login Page](login_screen_path_placeholder) <!-- Можно добавить скриншот позже -->

## ✨ Возможности

- **🗣 Чат с ИИ**: Поддержка markdown, подсветка кода, история сообщений.
- **🔄 Очереди запросов**: Система очередей (QueueService) гарантирует стабильную работу даже с однопоточными LLM.
- **⚡️ Real-time статусы**: Индикация генерации, непрочитанные сообщения.
- **🛑 Умная отмена**: Server-Side Cancellation позволяет мгновенно прервать генерацию без "мусорных" ответов.
- **🔐 Безопасность**: JWT-сессии, защищенные маршруты, хэширование паролей.
- **📱 Адаптивность**: Полностью отзывчивый интерфейс (Mobile/Desktop).

## 🛠 Технологический стек

- **Frontend**: React (Vite), Tailwind CSS, Lucide Icons.
- **Backend**: Node.js, Express, PostgreSQL.
- **AI Integration**: n8n Webhook / Ollama.
- **Infrastructure**: Docker, Docker Compose.

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- (Опционально) Node.js v20+ для локальной разработки

### Запуск

1.  **Клонируйте репозиторий**:

    ```bash
    git clone https://github.com/your-username/local-chat.git
    cd local-chat
    ```

2.  **Запустите проект**:

    ```bash
    docker-compose up -d --build
    ```

    _Эта команда соберет образы frontend/backend и запустит базу данных._

3.  **Откройте приложение**:

    - Frontend: [http://localhost:5173](http://localhost:5173)
    - Backend API: [http://localhost:3000](http://localhost:3000)

4.  **Вход**:
    - **Логин**: `admin`
    - **Пароль**: `admin123`

## 📂 Структура проекта

```
local-chat/
├── backend/            # Express.js API
│   ├── src/
│   │   ├── services/   # QueueService, LLMService
│   │   ├── routes/     # API Endpoints
│   │   └── db/         # PostgreSQL connection
├── frontend/           # React App
│   ├── src/
│   │   ├── components/ # UI Components (Chat, Sidebar...)
│   │   └── pages/      # Route Pages
├── docker/             # Docker configs & migrations
│   ├── init.sql        # Схема БД
│   └── migrations/     # SQL миграции
├── README/             # Детальная документация
│   ├── Roadmap.md      # План разработки
│   ├── Journal.md      # Журнал изменений
│   └── PRD_*.md        # Требования
└── docker-compose.yml  # Оркестрация контейнеров
```

## 📚 Документация

Вся актуальная информация о структуре и работе проекта находится в итоговой документации:

- **[Итоговая документация (Final Documentation)](README/Final_Documentation.md)** — **Рекомендуется к чтению**

Архивные документы (могут отличаться от реализации):

- [Roadmap & Прогресс](README/Roadmap.md)
- [Журнал изменений](README/Journal.md)
- [PRD (Архив)](README/PRD_idea.md)

## 🔧 Разработка

### Пересборка контейнеров

Если вы внесли изменения в код:

```bash
docker-compose up -d --build
```

Для полного перезапуска (с удалением томов, кроме БД):

```bash
docker-compose down && docker-compose up -d --build
```

### Переменные окружения (.env)

Основные настройки находятся в `docker-compose.yml`.

- `SESSION_SECRET`: Секрет для сессий
- `N8N_WEBHOOK_URL`: URL вебхука n8n
- `N8N_API_KEY`: API ключ (если используется)

---

## 🚀 Деплой (Production)

Для запуска на сервере используется отдельная конфигурация `docker-compose.prod.yml` и оптимизированные Docker-образы.

### 1. Подготовка на сервере

1.  Установите Docker и Docker Compose.
2.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/bn-skn/local-chat.git
    cd local-chat
    ```
3.  Переключитесь на ветку `main` (если вы не на ней).

### 2. Настройка окружения

Создайте файл `.env` на основе примера:

```bash
cp .env.production.example .env
nano .env
```

**Важно**: Обязательно измените пароли и секретные ключи!

### 3. Запуск

```bash
docker-compose -f docker-compose.prod.yml up -d --build
```

- Frontend будет доступен на порту `80`.
- Backend (API) недоступен снаружи напрямую (только через Nginx внутри сети Docker), если не открыты порты специально.

### 4. Обновление

```bash
git pull origin main
docker-compose -f docker-compose.prod.yml up -d --build
```

### 5. Запуск на поддомене или в подпапке

Если вы хотите запустить приложение не в корне домена (например, `example.com/chat`), добавьте переменную `VITE_BASE_PATH` в ваш `.env` файл перед запуском:

```bash
VITE_BASE_PATH=/chat/
```

Приложение автоматически подхватит этот путь при сборке.

### 6. Смена порта (если 80 занят)

Если вы хотите запустить приложение на другом порту (например, 8080), добавьте переменную `APP_PORT` в `.env`:

```bash
APP_PORT=8080
```

После перезапуска (`docker-compose -f docker-compose.prod.yml up -d`), приложение будет доступно по адресу `http://{server-ip}:8080`.

---

Created by Google Antigravity Agent.
