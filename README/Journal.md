# Журнал работ — Local Chat

## 2026-01-09
- **Восстановление и Финализация**:
  - Полная пересборка контейнеров после очистки.
  - Проверка работоспособности всех систем (Login, Chat, History).
  - Обновление всей проектной документации.

## 2026-01-07
- **Server-Side Cancellation**:
  - Реализован механизм "честной" отмены генерации.
  - Добавлен метод `QueueService.cancel(chatId)`.
  - Кнопка "Стоп" теперь предотвращает сохранение ненужного ответа в БД.
- **Queue Service (Очереди)**:
  - Внедрен `QueueService` для последовательной обработки запросов к LLM (single-threaded limitation workaround).
  - Реализован статус чата `generating` и блокировка отправки новых сообщений.
  - Добавлен polling статуса на фронтенде.
- **UI/UX Improvements**:
  - Восстановлена анимация "три точки" (Loader) при генерации.
  - Добавлены иконки статуса в сайдбаре (generating/unread).
  - Исправлен баг с исчезающей кнопкой отмены.
- **Bug Fixes**:
  - Исправлена критическая ошибка входа (SyntaxError в `chats.js`).
  - Исправлена проблема авторизации (мигание).

## 2026-01-06
- **Проверка MCP серверов**:
  - Подтверждена работоспособность `context7`.
  - Подтверждена работоспособность `shadcn-ui-server`.
  - Все инструменты готовы к работе.
